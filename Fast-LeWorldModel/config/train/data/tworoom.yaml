dataset:
  num_steps: ${eval:'${wm.dense_supervision.max_transitions} + 1'}
  frameskip: 1
  name: tworoom
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio