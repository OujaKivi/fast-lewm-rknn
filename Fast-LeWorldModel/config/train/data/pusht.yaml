dataset:
  num_steps: ${eval:'${wm.dense_supervision.max_transitions} + 1'}
  frameskip: 1
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state