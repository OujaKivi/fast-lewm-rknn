dataset:
  name: cube_single_expert

  num_steps: ${eval:'${wm.dense_supervision.max_transitions} + 1'}
  frameskip: 1
  keys_to_load:
    - pixels
    - action
    - observation
  keys_to_cache:
    - action
    - observation
  keys_to_merge:
    proprio: proprio