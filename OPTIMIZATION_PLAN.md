# Fast-LeWM 端侧异构优化计划书

**创建时间**：2026-09-18
**目标**：优化 NPU 数据搬运开销，实现真正的端侧异构加速

---

## 一、当前状态

### 现有性能基线（10个随机episode，3次规划限制）

| 指标 | CPU 模式 | NPU 模式（当前） |
|------|---------|----------------|
| 成功率 | 60% | 60% |
| 规划时间 | 17.92s | 19.05s |
| NPU 相对 CPU | - | 慢 6.3% |

### 当前 NPU 模式架构

```
CPU ViT 编码 → CPU action_encoder → CPU→NPU 拷贝 → NPU predictor → NPU→CPU 拷贝 → CPU CEM 更新
```

**瓶颈分析**：数据搬运开销约 8s/次规划，占总时间的 42%

---

## 二、优化任务

### P0：ViT 上 NPU 性能测试 ✅

**目标**：测试 ViT 视觉编码器在 NPU 上的性能，判断是否有加速收益

**步骤**：
1. ✅ 将 ViT 模型导出为 ONNX
2. ✅ 在 x86 机器上转换为 RKNN 格式
3. ✅ 板端测试 ViT NPU 推理性能
4. ✅ 对比 CPU vs NPU ViT 推理时间

**测试结果**：
- ViT CPU 推理时间（batch=1）：**136.48 ms**
- ViT NPU 推理时间（batch=1）：**56.24 ms**
- **加速比：2.43x**

**端到端收益估算**：
- 一次规划需要 30 步 CEM × 2 次图像编码（cur + goal）= 60 次 ViT 推理
- CPU ViT 总耗时：60 × 136.48ms = **8.19s**
- NPU ViT 总耗时：60 × 56.24ms = **3.37s**
- **节省：4.82s**
- 端到端预期：18.97s → **14.15s**（加速 ~25%）

**结论**：ViT 上 NPU 有显著收益，值得集成！

**验证标准**：ViT NPU 推理时间 < ViT CPU 推理时间 ✅

---

### P1：融合 pred_proj 到 NPU 模型

**目标**：将 pred_proj（输出投影层）融合到 NPU 模型中，减少一次 CPU↔NPU 拷贝

**当前问题**：
- NPU predictor 输出 latent (B,5,192)
- 拷贝到 CPU 后，用 pred_proj 投影到 score
- 再拷贝回 CPU 做 CEM 更新

**优化方案**：
- 将 pred_proj 融合到 NPU 模型中
- NPU 直接输出 score，不需要拷贝回 CPU 做投影

**步骤**：
1. 修改 ONNX 导出脚本，包含 pred_proj
2. 在 x86 机器上重新转换 RKNN
3. 板端测试融合后的 NPU 模型
4. 对比优化前后的端到端时间

**预期收益**：
- 减少一次 CPU↔NPU 数据拷贝（~1-2s）
- 端到端加速约 5-10%

**验证标准**：端到端规划时间减少 > 5%

---

### P2：INT8 量化

**目标**：将 predictor 模型量化为 INT8，减少数据搬运量和计算时间

**当前状态**：
- NPU 模型是 FP16 精度
- 输入输出都是 FP16，数据搬运量大

**优化方案**：
- 使用 RKNN 的 INT8 量化工具
- 用校准数据集（从训练集采样）做量化校准
- 板端测试 INT8 模型性能和精度损失

**步骤**：
1. 准备校准数据集（从训练集采样 100-1000 个样本）
2. 在 x86 机器上做 INT8 量化转换
3. 板端测试 INT8 NPU 推理性能
4. 测试 INT8 模型的精度损失（reward 差异）

**预期收益**：
- 数据量减少 50%（FP16 → INT8）
- NPU 计算速度提升 2-3x（INT8 是 NPU 的原生精度）
- 端到端加速约 10-20%

**风险**：
- 量化可能导致精度损失
- 需要验证 reward 差异 < 1%

**验证标准**：
- INT8 NPU 推理时间比 FP16 快 > 30%
- 成功率保持一致（60%）
- reward 差异 < 1%

---

### P3：真正的异构调度

**目标**：根据各组件特性分配到 CPU/NPU，实现最优异构调度

**各组件特性分析**：

| 组件 | 计算量 | 数据量 | 适合设备 |
|------|--------|--------|---------|
| ViT | 大（12层 Transformer） | 小（224x224x3） | NPU（如果 P0 验证有收益） |
| Action Encoder | 小（3层 Transformer） | 极小（5x10） | CPU（小模型，NPU 开销大） |
| Predictor | 大（6层 Transformer） | 中（300x5x192） | NPU（大 batch 并行） |
| CEM | 小（采样+TopK） | 小 | CPU（逻辑操作） |

**优化方案**：
```
NPU ViT 编码 → CPU action_encoder → CPU→NPU 拷贝 → NPU predictor + pred_proj → NPU→CPU 拷贝 → CPU CEM 更新
```

**步骤**：
1. 集成 P0（ViT NPU）、P1（融合 pred_proj）、P2（INT8 量化）的成果
2. 实现异构调度 pipeline
3. 测试完整端到端性能
4. 对比优化前后的性能和精度

**预期收益**：
- 综合加速 15-25%
- 端到端规划时间从 19.05s 降到 14-16s

**验证标准**：
- 端到端规划时间比纯 CPU 快 > 10%
- 成功率保持一致（60%）
- reward 差异 < 1%

---

## 三、进度跟踪

| 任务 | 状态 | 开始时间 | 完成时间 | 结果 |
|------|------|---------|---------|------|
| P0: ViT NPU 性能测试 | ✅ 已完成 | 2026-09-18 | 2026-09-18 | NPU 加速 2.19x（122ms→55.6ms） |
| P1: 融合 pred_proj | ⏳ 待开始 | - | - | - |
| P2: INT8 量化 | ⏳ 待开始 | - | - | - |
| P3: 异构调度 | ⏳ 待开始 | - | - | - |

---

## 四、风险与应对

| 风险 | 影响 | 应对方案 |
|------|------|---------|
| ViT NPU 没有加速收益 | P0 失败 | 保留 ViT 在 CPU 上 |
| INT8 量化精度损失大 | P2 失败 | 退回 FP16，或做混合精度量化 |
| 数据搬运开销仍然很大 | P3 收益不达预期 | 研究 zero-copy 或共享内存 |

---

## 五、当前环境信息

### RK3588 板子
- IP: 192.168.77.2, user: root, 密码: 123456
- Ubuntu 22.04/aarch64/16GB
- NPU: 三核 6TOPS INT8, Runtime 2.3.2
- 锁频: 小核 1800MHz, 大核 2352MHz, NPU 1GHz

### x86 转换机
- IP: 10.128.201.131, user: wjw, 密码: wjww118xxx
- conda 环境: rknn-py310 (torch 2.4.0+cpu, rknn-toolkit2 2.3.2)
- 约束: 未经允许不要用 docker

### Mac 环境
- Apple M5 Pro (arm64)
- 项目目录: /Users/wangjiwei/Doubao/chats/2026-09-14/new-chat-2/
